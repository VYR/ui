import { HasAccessToUserDirective } from './has-access-to-user.directive';

describe('HasAccessToUserDirective', () => {
    it('should create an instance', () => {
        const directive = new HasAccessToUserDirective();
        expect(directive).toBeTruthy();
    });
});
