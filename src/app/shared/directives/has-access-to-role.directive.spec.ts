import { HasAccessToRoleDirective } from './has-access-to-role.directive';

describe('HasAccessToRoleDirective', () => {
    it('should create an instance', () => {
        const directive = new HasAccessToRoleDirective();
        expect(directive).toBeTruthy();
    });
});
