import { HasEntitlementAccessTo } from './has-entitlement-access-to.directive';

describe('HasEntitlementAccessTo', () => {
    it('should create an instance', () => {
        const directive = new HasEntitlementAccessTo();
        expect(directive).toBeTruthy();
    });
});
